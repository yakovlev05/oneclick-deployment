spring:
  application:
    name: oneclick-deployment
  datasource:
    url: jdbc:postgresql://${POSTGRES_HOST:localhost}:${POSTGRES_PORT:5432}/${POSTGRES_DB:oneclick-deployment}
    username: ${POSTGRES_USER:user}
    password: ${POSTGRES_PASSWORD:password}
  flyway:
    enabled: true
